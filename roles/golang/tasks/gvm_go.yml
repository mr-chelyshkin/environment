---
- block:
  - name: "Install go{{ golang_version }} via GVM"
    command:
      cmd: "{{ ansible_env.HOME }}/.gvm/bin/gvm install go1.4"
    environment:
      GVM_ROOT: "{{ ansible_env.HOME }}/.gvm"
  - name: "Select {{ golang_version }}"
    command:
      cmd: "{{ ansible_env.HOME }}/.gvm/bin/gvm use go1.4 --default"
    environment:
      GVM_ROOT: "{{ ansible_env.HOME }}/.gvm"
